export default function TrendChart() {
  // Placeholder — will be replaced with Recharts line chart
  return (
    <div style={{ padding: 16, border: '1px solid #ddd', borderRadius: 8, background: '#fff' }}>
      <h4 style={{ margin: '0 0 12px' }}>OEE Trend (24h)</h4>
      <div style={{ height: 200, display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#999' }}>
        Chart will render here — requires Recharts integration
      </div>
    </div>
  )
}
